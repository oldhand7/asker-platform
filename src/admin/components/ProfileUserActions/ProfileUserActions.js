import {
    TopToolbar,
    CreateButton
} from 'react-admin';

const ProfileUserActions = () => {
  return <TopToolbar>
          <CreateButton/>
      </TopToolbar>
}

export default ProfileUserActions;

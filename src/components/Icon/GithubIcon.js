import Icon from './Icon';
import GithubSvg from './assets/icons/teenyicons_github-solid.svg';

const GithubIcon = ({ className }) => <Icon icon={<GithubSvg />} className={className} />;

export default GithubIcon;
